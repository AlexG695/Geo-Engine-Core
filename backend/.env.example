# ==========================================
# 🗺️ Geo Engine Core - Configuración de Entorno
# ==========================================
# INSTRUCCIONES:
# 1. Copia este archivo: cp .env.example .env
# 2. Ajusta los valores según tu entorno local.
# ==========================================

# --- Base de Datos (PostgreSQL + PostGIS) ---
# Formato: postgres://user:password@host:port/dbname?sslmode=disable
# Nota: Para Docker usa host 'db'. Para local usa 'localhost'.
DBConnection=postgres://geo:password@db:5432/geoengine?sslmode=disable

# --- Seguridad ---
# ⚠️ IMPORTANTE: Cambia esto por una cadena aleatoria y segura.
# Esta llave se usa para validar los headers X-Geo-Key y conexiones WebSocket.
API_SECRET=change_me_to_a_secure_random_key

# --- Redis (Cache & Rate Limiting) ---
# Para Docker usa 'redis:6379'. Para local 'localhost:6379'.
REDIS_ADDR=redis:6379
# Deja vacío si no usas contraseña en desarrollo
REDIS_PASSWORD=

# --- Entorno ---
# Opciones: development | production
# 'development' activa logs detallados y formato legible.
ENV_MODE=development

# --- CORS (Frontend) ---
# URL permitida para conectar a la API (Vite por defecto usa puerto 5173)
ALLOWED_ORIGINS=http://localhost:5173